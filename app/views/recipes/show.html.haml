= render 'commons/header'
.container    
  .recipe-row
    .recipe-intro
      .recipe-title
        %p= @recipe.title
      .recipe-url
        %a= @recipe.url
      .recipe-info
        %span= @recipe.info
      .recipe-content
        %p= @recipe.content
  = link_to '編集', edit_recipe_path
  = link_to '評価', new_recipe_assessment_path(@recipe.id)

= render 'commons/footer'
